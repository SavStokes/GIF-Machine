# GIF-Machine
GIF Machine is a Java-based web server that allows user to generate and download random GIFs from the Tenor API.  

How to Run  
javac -cp gson-2.13.1.jar GIFMachine.java  
java -cp .;gson-2.13.1.jar GIFMachine
